{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "video-at-scale",
  "compatibility_date": "2026-08-07",
  "compatibility_flags": ["nodejs_compat"],
  "main": "src/server.ts",
  "observability": {
    "enabled": true,
  },
  // Reconcile video_job rows stuck at 'running' (see src/lib/jobReconcile.ts)
  "triggers": {
    "crons": ["*/5 * * * *"],
  },
  "durable_objects": {
    "bindings": [
      {
        "name": "JOB_ROOM",
        "class_name": "JobRoom",
      },
    ],
  },
  "migrations": [
    {
      "tag": "v1",
      "new_sqlite_classes": ["JobRoom"],
    },
  ],
  "r2_buckets": [
    {
      "binding": "VIDEOS",
      "bucket_name": "video-at-scale-videos",
      "preview_bucket_name": "video-at-scale-videos-preview",
    },
  ],
  // Cloudflare Email Service (onboard a domain first). Use remote:true for real send in wrangler dev.
  "send_email": [
    {
      "name": "EMAIL",
    },
  ],
  // Optional Hyperdrive (create with: wrangler hyperdrive create …)
  // Uncomment after creating a config; edge will prefer HYPERDRIVE over DATABASE_URL.
  // "hyperdrive": [
  //   {
  //     "binding": "HYPERDRIVE",
  //     "id": "<hyperdrive-config-id>",
  //   },
  // ],
  // Runtime secrets are projected from Doppler prd via:
  //   doppler run -- … bun run secrets:sync-edge
  // (CI deploy-edge does this before wrangler deploy.) Keys: DATABASE_URL,
  // BETTER_AUTH_*, EMAIL_*, STATUS_WEBHOOK_SECRET, TEMPORAL_STARTER_*.
  // EMAIL_MODE is a secret (prd=cf); local .dev.vars sets console.
  "vars": {
    "APP_NAME": "video-at-scale",
  },
}
